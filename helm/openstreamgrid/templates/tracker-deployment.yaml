{{- if and (eq .Values.tracker.env.STORE_TYPE "sqlite") (gt (int .Values.tracker.replicaCount) 1) }}
{{- fail "tracker.replicaCount must be 1 when STORE_TYPE is sqlite" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openstreamgrid.trackerName" . }}
  labels:
    {{- include "openstreamgrid.labels" . | nindent 4 }}
    app.kubernetes.io/component: tracker
spec:
  replicas: {{ .Values.tracker.replicaCount }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      {{- include "openstreamgrid.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: tracker
  template:
    metadata:
      labels:
        {{- include "openstreamgrid.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: tracker
    spec:
      securityContext:
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: tracker
          image: "{{ .Values.tracker.image.repository }}:{{ .Values.tracker.image.tag }}"
          imagePullPolicy: {{ .Values.tracker.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          env:
            - name: PORT
              value: {{ .Values.tracker.port | quote }}
            - name: STORE_TYPE
              value: {{ .Values.tracker.env.STORE_TYPE | quote }}
            - name: DB_PATH
              value: {{ .Values.tracker.env.DB_PATH | quote }}
            {{- if .Values.tracker.auth.existingSecret }}
            - name: TRACKER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tracker.auth.existingSecret | quote }}
                  key: {{ .Values.tracker.auth.apiKeyKey | quote }}
            - name: PEER_SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tracker.auth.existingSecret | quote }}
                  key: {{ .Values.tracker.auth.peerSessionSecretKey | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.tracker.port }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 5
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.tracker.resources | nindent 12 }}
          volumeMounts:
            - name: tracker-data
              mountPath: /data
      terminationGracePeriodSeconds: 10
      volumes:
        - name: tracker-data
          {{- if .Values.tracker.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "openstreamgrid.trackerName" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
