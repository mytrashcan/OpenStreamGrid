{
  "name": "openstreamgrid",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "common",
    "tracker",
    "origin",
    "peer"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present && npm --prefix sdk run build",
    "test": "npm run test --workspaces --if-present && npm --prefix sdk test",
    "test:docker-health": "bash test/docker-health-test.sh",
    "typecheck": "npm run typecheck --workspaces --if-present && npm --prefix sdk run typecheck"
  },
  "devDependencies": {
    "@types/node": "24.13.2",
    "@typescript-eslint/eslint-plugin": "^8.64.0",
    "@typescript-eslint/parser": "^8.64.0",
    "eslint": "^10.7.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
